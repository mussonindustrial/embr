{
    "name": "@embr-js/sse",
    "private": true,
    "version": "0.4.1",
    "scripts": {
        "dev": "vite",
        "build": "tsc && nx vite:build && nx vite:test --run",
        "clean": "",
        "test": "nx vite:test --run",
        "bench": "nx vite:test --bench",
        "preview": "vite preview",
        "lint": "eslint src/**/*.ts",
        "lint:prettier": "prettier -c . --cache --ignore-path=../../.prettierignore"
    },
    "dependencies": {
        "eventsource": "4.0.0",
        "fetch-retry": "6.0.0"
    },
    "devDependencies": {
        "@types/eventsource": "3.0.0"
    }
}
