{
  "type": "object",
  "properties": {
    "first": {
      "type": "boolean",
      "description": "True is this is the first time the event has fired."
    },
    "chart": {
      "type": "object",
      "description": "JavaScriptProxy for the `chart` object. Allows running JavaScript code in the client with the chart as the `this` context.",
      "properties": {
        "runAsync": {
          "type": "object",
          "description": "Run JavaScript code asynchronously."
        },
        "runBlocking": {
          "type": "object",
          "description": "Run blocking JavaScript code."
        }
      }
    },
    "memo": {
      "type": "object",
      "description": "Memoization object that allows storing state between event executions. This dictionary is created once at initialization and used for all following events."
    }
  }
}